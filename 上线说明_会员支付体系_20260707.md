# 上线说明：会员支付体系 2026-07-07

## 这次更新内容

1. 首页套餐展示更新为：月度会员、季度会员、年度会员、15 次加量包。
2. 支付页支持四个产品：`monthly`、`quarterly`、`yearly`、`addon_15`。
3. 支付成功回调后自动发放会员权益或加量包次数。
4. 会员中心保留原 HR 左侧导航风格，新增订单记录展示。
5. 创建正式候选人链接时检查面试额度；额度不足时引导购买会员或加量包。
6. 未支付订单只作为临时支付记录；超过 24 小时未支付会自动删除，会员中心订单记录只展示已支付成功订单。

## 必须先执行数据库升级

在宝塔数据库里，选择 `hi_interview` 数据库，执行：

```sql
api/upgrade_20260707_membership_billing.sql
```

如果之前订单表升级脚本还没有执行，也要先执行：

```sql
api/upgrade_20260622_orders_invoices.sql
api/upgrade_20260702_real_payment.sql
```

## 上传注意

1. 本包不包含 `api/config.php`，不要覆盖线上配置。
2. 本包不包含支付宝/微信 PEM 密钥文件，线上原有密钥不要删除。
3. 上传时覆盖同名文件即可，不要先清空网站目录。
4. 支付回调依赖线上 `api/config.php` 里的支付配置。

## 产品规则

| 产品 | 价格 | 周期 | 次数 | 规则 |
|---|---:|---:|---:|---|
| 月度会员 | 199 元 | 30 天 | 20 次 | 到期清零，不累计 |
| 季度会员 | 597 元 | 90 天 | 75 次 | 周期内不清零，续费累计剩余次数 |
| 年度会员 | 2388 元 | 365 天 | 365 次 | 含赠送 5 次，续费累计剩余次数 |
| 加量包 | 99 元 | 长期有效 | 15 次 | 仅有效会员可购买，不增加会员天数 |
| 免费用户 | 0 元 | 自然月 | 5 次 | 每月清零 |

## 关键接口

- `api/hr/create_order.php`：创建支付订单
- `api/payment/alipay_notify.php`：支付宝回调并发放权益
- `api/payment/wechat_notify.php`：微信回调并发放权益
- `api/hr/membership_status.php`：会员中心和额度检查
- `api/hr/order_list.php`：会员中心订单记录
- `api/hr/order_detail.php`：支付成功页订单详情
- `api/hr/create_invite.php`：生成候选人链接并扣减额度

## 上线后自测

1. 打开首页，确认套餐展示为月度/季度/年度/加量包。
2. 登录 HR 后进入会员中心，确认左侧导航正常、订单记录区域正常。
3. 进入支付页，分别选择微信/支付宝，确认能创建订单。
4. 完成一笔小额真实支付或测试支付，确认会员中心次数增加。
5. 创建初面并生成链接，确认会扣减 1 次额度。
